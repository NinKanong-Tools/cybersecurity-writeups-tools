# Unicornscan

![hq720](https://github.com/user-attachments/assets/896c7cb9-8d8b-459b-95a5-e866c1534dc2)

### what is Unicornscan?
unicornscan is a network reconnaissance and security testing tool. It’s designed to be asynchronous, meaning it can handle a large number of concurrent connections and scans very efficiently. It’s often used for tasks like port scanning, service identification, and network mapping.

### - What unicornscan Does:
unicornscan is capable of performing various network scans, including:
    - TCP SYN scans: The most common type of port scan, used to determine if ports are open.
    - TCP connect scans: A more thorough scan that completes the three-way TCP handshake.
    - UDP scans: Used to discover open UDP ports.
    - Service version detection: Identifies the version of services running on open ports.
    - Operating system fingerprinting: Tries to determine the operating system of the target host.
    - Network mapping: Discovers hosts and network topology.
    - HTTP/HTTPS probing: Retrieves information from web servers.

## lab tools:
- Kali Linux
- metasploitable2

## Verify
````
unicornscan --version
````
<img width="893" height="129" alt="image" src="https://github.com/user-attachments/assets/9790d4c0-7a85-4f4a-8c71-afbf894fc806" />

- In this i don't install it cause it already have in kali.
- If it don't have in kali, following me to install:
````
sudo apt install unicornscan
````
<img width="767" height="224" alt="image" src="https://github.com/user-attachments/assets/6dcc3836-61a2-4959-98c7-ae17a962649e" />


##  Get help:
````
unicornscan -h
````
<img width="889" height="523" alt="image" src="https://github.com/user-attachments/assets/eb482781-6db8-4961-b018-2cf9ea77770a" />

### Usage:
| Option                  | Description                                                                                                          |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `-b, --broken-crc`      | Set broken CRC sums on the Transport layer (T), Network layer (N), or both (TN).                                     |
| `-B, --source-port`     | Set the source port for the scan.                                                                                    |
| `-c, --proc-duplicates` | Process duplicate replies.                                                                                           |
| `-d, --delay-type`      | Set the delay type (1 = TSC, 2 = GTOD, 3 = sleep).                                                                   |
| `-D, --no-defpayload`   | Disable the default payload; only probe known protocols.                                                             |
| `-e, --enable-module`   | Enable specific modules (e.g., output and report modules).                                                           |
| `-E, --proc-errors`     | Process non-open responses (ICMP errors, TCP RSTs, etc.).                                                            |
| `-F, --try-frags`       | Attempt to fragment packets.                                                                                         |
| `-G, --payload-group`   | Specify the payload group for TCP/UDP payload selection (default = all).                                             |
| `-h, --help`            | Display help information.                                                                                            |
| `-H, --do-dns`          | Resolve hostnames during the reporting phase.                                                                        |
| `-i, --interface`       | Specify the network interface to use (e.g., eth0).                                                                   |
| `-I, --immediate`       | Enable immediate mode; display results as they are found.                                                            |
| `-j, --ignore-seq`      | Ignore sequence numbers for TCP header validation (A = all, R = reset).                                              |
| `-l, --logfile`         | Write output to a specified log file instead of the terminal.                                                        |
| `-L, --packet-timeout`  | Set the timeout for waiting for packets (default = 7 seconds).                                                       |
| `-m, --mode`            | Specify the scan mode: TCP SYN (T), UDP (U), TCP connect (sf), ARP (A), or TCP flags after T (e.g., -mTsFpU).        |
| `-M, --module-dir`      | Specify the directory where modules are located (default = `/usr/lib/unicornscan/modules`).                          |
| `-o, --format`          | Specify the output format for replies.                                                                               |
| `-p, --ports`           | Specify global ports to scan (if not specified in target options).                                                   |
| `-P, --pcap-filter`     | Add an extra PCAP filter string for the receiver.                                                                    |
| `-q, --covertness`      | Set covertness value (0–255).                                                                                        |
| `-Q, --quiet`           | Disable output to the screen (useful for logging).                                                                   |
| `-r, --pps`             | Set the packets per second (total, not per host).                                                                    |
| `-R, --repeats`         | Repeat the packet scan N times.                                                                                      |
| `-s, --source-addr`     | Set the source address for packets (r = random).                                                                     |
| `-S, --no-shuffle`      | Disable port shuffling.                                                                                              |
| `-t, --ip-ttl`          | Set TTL (Time to Live) for sent packets (e.g., 62, 6–16, r64–128).                                                   |
| `-T, --ip-tos`          | Set the TOS (Type of Service) for sent packets.                                                                      |
| `-u, --debug`           | Set the debug mask.                                                                                                  |
| `-U, --no-openclosed`   | Do not display “open” or “closed” status.                                                                            |
| `-w, --safefile`        | Write a PCAP file of received packets.                                                                               |
| `-W, --fingerprint`     | Perform OS fingerprinting: 0=Cisco, 1=OpenBSD, 2=Windows XP, 3=p0fsendsyn, 4=FreeBSD, 5=Nmap, 6=Linux, 7=StrangeTCP. |
| `-v, --verbose`         | Enable verbose output (multiple -v increase verbosity).                                                              |
| `-V, --version`         | Display the version of Unicornscan.                                                                                  |
| `-z, --sniff`           | Enable sniffing mode.                                                                                                |
| `-Z, --drone-str`       | Specify a drone string.                                                                                              |

## 1. Scan a Single Host (all open TCP ports)
````
sudo unicornscan -Iv 192.168.127.129
````
<img width="895" height="328" alt="image" src="https://github.com/user-attachments/assets/2f9c7eec-2ed4-4467-81b6-66db28acf719" />

- `-I` : show IP info.
- `-v` : verbose.

## 2. Scan a Specific Port
- Retry the Scan:
````
sudo unicornscan -mT -v -p22 192.168.127.129
````
<img width="823" height="132" alt="image" src="https://github.com/user-attachments/assets/17be324c-7943-4732-9949-fc6b69ebab6d" />

- `-mT` : Uses TCP mode with a "many ports" technique for speed.
- `-v` : Enables verbose output.


- Add Timeout or Rate Control:

<img width="824" height="130" alt="image" src="https://github.com/user-attachments/assets/c386d54c-790c-47d1-a6c9-3b858d869863" />

- `-r 100` : sets the rate to 100 pps, which might reduce interruptions.
 

## 3. Scan Multiple Ports
````
sudo unicornscan -mT -p22,80,443 192.168.127.129
````
<img width="823" height="156" alt="image" src="https://github.com/user-attachments/assets/4ac76ee0-40a3-46a2-b7f1-9f4d2d6588dc" />

- This command it will checks SSH, HTTP, HTTPS.


## 4. Scan a Subnet
````
sudo unicornscan -mT 192.168.127.0/24
````
<img width="679" height="112" alt="image" src="https://github.com/user-attachments/assets/82d7116e-0b56-4a8c-bdab-08d871c0596f" />

- This command will finds all hosts alive with open ports in the subnet.


## 5. UDP Scan
````
unicornscan -mU -v -p53,161 192.168.127.129
````
<img width="862" height="165" alt="image" src="https://github.com/user-attachments/assets/cca7b437-3754-4f4f-8650-692d674c5b2b" />

- `-mU` : Specifies UDP mode for scanning.
- `-v` : Verbose.
- This command will checks DNS (53) and SNMP (161) on target.


## 6. Full Port Range (fast scan)
````
sudo unicornscan -mT -p1-65535 192.168.127.129 -r5000
````
<img width="666" height="534" alt="image" src="https://github.com/user-attachments/assets/d67146b4-3f6c-4e9b-968a-37ac1939b831" />

- `-r5000` = 5000 packets per second.
- `-p1-65535` : scan port from 1 until 65535


## 7. Banner Grabbing
````
sudo unicornscan -I eth0 -v -mT -p80 -B 12345 192.168.127.129
````
<img width="819" height="165" alt="image" src="https://github.com/user-attachments/assets/e5edae05-e2bc-4ac6-9416-2e2ac753c58e" />

- `-I eth0` : Specifies the network interface.
- `-B 12345` : Sets the source port to 12345.
- -Iv: Likely intended as -I (interface) and -v (verbose), but combined incorrectly.



## 8. fast TCP reconnaissance scan
````
sudo unicornscan -v -I -mT 192.168.127.129
````
<img width="896" height="633" alt="image" src="https://github.com/user-attachments/assets/1774cbed-5282-4e1b-9aef-2570e96b20e1" />

<img width="757" height="339" alt="image" src="https://github.com/user-attachments/assets/ba86b3dd-60ad-43d2-9651-d850f44db6e1" />

- `-v` : Verbose mode shows more details about what Unicornscan is doing.
- `-I` : Immediate mode  prints results as soon as a response is received.
- This command will Scans all TCP ports (by default 1–65535) on the host 192.168.127.129.


## 9. fast UDP reconnaissance scan
````
sudo unicornscan -v -I -mU 192.168.127.129
````
<img width="874" height="245" alt="image" src="https://github.com/user-attachments/assets/7efb4bca-5fcf-483c-8cee-e941183eea2c" />

- This command use to scan UDP port


## 10. Scans the default port range
- Specific IP
````
sudo unicornscan -r500 -mT -v -I 192.168.127.129
````
<img width="882" height="323" alt="image" src="https://github.com/user-attachments/assets/d796fef4-5d88-4e4b-97b6-37c359ad58a3" />

- `-r500` : Rate  sends 500 packets per second. Controls speed of scan.


- Scan range
````
sudo unicornscan -r500 -mT -v -I 192.168.127.1/24
````
<img width="920" height="285" alt="image" src="https://github.com/user-attachments/assets/8b0a034c-fdd7-4b3e-b8ff-f4089c4cd4a6" />




## 11. Scan a Subnet for a Specific Port
````
sudo unicornscan -r500 -mT -v -I 192.168.127.1/24:22
````
<img width="820" height="164" alt="image" src="https://github.com/user-attachments/assets/9e12a203-17e3-4511-87fe-ed9cb619667f" />



## 12. SYN + ACK TCP packets
````
sudo unicornscan -mTsA -v -I 192.168.127.129
````
<img width="894" height="315" alt="image" src="https://github.com/user-attachments/assets/a274dd9b-1a46-4146-a3b3-8895fd58da14" />

- `-mTsA` : TCP scan with flags S (SYN) and A (ACK). This sets the type of TCP packets sent.


## 13. multiple types of TCP packets (SYN, FIN, PUSH
````
sudo unicornscan -mTsFPU -v -I 192.168.127.129
````
<img width="891" height="297" alt="image" src="https://github.com/user-attachments/assets/ebd88ad3-7c66-4795-9a01-303ab68ac31b" />

- `-mTsFPU` : Scan TCP and UDP ports with multiple flags:



### ================>>> End Of unicornscan

### Why hacker use unicorn:
Hacker use unicornscan cause it has unique features that make it popular for recon and stealthy scanning.
- Super Fast Scanning
- Stealth & Evasion
- Flexible Scanning
- Real-Time Reporting



### Creditable:
1. https://www.youtube.com/watch?v=X_DdYUeKS-o
2. https://awjunaid.com/kali-linux/unicornscan-network-reconnaissance-and-port-scanner/


### Practice in LAB BY: Nin Kanong(k4n0ng)

#### Date: 31/Aug/2025
